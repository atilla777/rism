sessions_trim:
  cron: "*/3 * * * *" # clear old (older than 30 days) session data (cookies) from DB
  class: 'SessionsTrimJob'
  queue: 'default'
  args:
    queue: 'default'
sync_nvd_base:
  cron: "*/30 * * * *" # execute local NVD base synchronization at every 30 minutes
  class: 'SyncNvdBaseJob'
  queue: 'sync_nvd_base'
  args:
    queue: 'sync_nvd_base'
